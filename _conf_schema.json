{
  "default_timeout": {
    "description": "Probe timeout",
    "type": "int",
    "hint": "Recommended: 30-120",
    "default": 60
  },
  "page_provider_id": {
    "description": "Page provider ID",
    "type": "string",
    "hint": "Example: openai-default. Leave empty to use the current/default provider when AstrBot exposes one.",
    "default": ""
  },
  "enable_protocol_probe": {
    "description": "Protocol probes",
    "type": "bool",
    "default": true
  },
  "enable_token_probe": {
    "description": "Token probes",
    "type": "bool",
    "hint": "Full audits always enable token probes even when this is false.",
    "default": false
  },
  "enable_fingerprint_probe": {
    "description": "Fingerprint probes",
    "type": "bool",
    "hint": "Full audits always enable fingerprint probes even when this is false.",
    "default": false
  },
  "fingerprint_profile": {
    "description": "Fingerprint breadth",
    "type": "string",
    "hint": "light | standard | exhaustive",
    "default": "standard"
  },
  "fingerprint_repeats": {
    "description": "Probe repeats",
    "type": "int",
    "hint": "Recommended: 1-5. Higher values improve mixed-route detection but cost more requests.",
    "default": 3
  },
  "enable_auxiliary_llm_judge": {
    "description": "Auxiliary judge",
    "type": "bool",
    "hint":"Deterministic scoring remains authoritative. Auxiliary LLMs are only used for deep fingerprint matching when available.",
    "default": false
  },
  "auxiliary_judge_mode": {
    "description": "Auxiliary judge source",
    "type": "string",
    "hint": "astrbot | openai_compatible",
    "default": "astrbot"
  },
  "auxiliary_judge_provider_id": {
    "description": "Judge provider ID",
    "type": "string",
    "hint": "Used when auxiliary_judge_mode is astrbot. Example: openai-judge-provider. Leave empty to use the page/current provider when available.",
    "default": ""
  },
  "auxiliary_judge_base_url": {
    "description": "Judge OpenAI-compatible Base URL",
    "type": "string",
    "hint": "Used when auxiliary_judge_mode is openai_compatible. Example: https://api.openai.com/v1",
    "default": ""
  },
  "auxiliary_judge_api_key": {
    "description": "Judge OpenAI-compatible API Key",
    "type": "string",
    "hint": "Used when auxiliary_judge_mode is openai_compatible.",
    "default": ""
  },
  "auxiliary_judge_model": {
    "description": "Judge OpenAI-compatible Model",
    "type": "string",
    "hint": "Used when auxiliary_judge_mode is openai_compatible. Example: gpt-4.1-mini",
    "default": ""
  },
  "enable_voluntary_data_reporting": {
    "description": "Allow optional data reporting",
    "type": "bool",
    "hint": "When enabled, official-endpoint audits may show a voluntary sanitized fingerprint contribution prompt after completion.",
    "default": false
  },
  "contribution_export_dir": {
    "description": "Voluntary contribution export directory",
    "type": "string",
    "hint": "Sanitized official-endpoint candidate packages are exported here when requested.",
    "default": "tmp/contributions"
  },
  "contribution_issue_url": {
    "description": "Voluntary contribution GitHub issue URL",
    "type": "string",
    "hint": "Issue creation URL for the trusted reference candidate repository.",
    "default": "https://github.com/zty-ui0215/astrbot_llm_identify_trusted_references/issues/new"
  },
  "enable_context_probe": {
    "description": "Context probes",
    "hint": "Reserved for future context-window sentinel probes. Currently disabled.",
    "type": "bool",
    "default": false
  },
  "strict_mode": {
    "description": "Strict mode",
    "hint": "Apply a more conservative confidence penalty for failed probes.",
    "type": "bool",
    "default": false
  }
}
